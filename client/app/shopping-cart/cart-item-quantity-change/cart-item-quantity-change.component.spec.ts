/*
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { CartItemQuntityChangeComponent } from './cart-item-quantity-change.component';

describe('CartItemQuntityChangeComponent', () => {
  let component: CartItemQuntityChangeComponent;
  let fixture: ComponentFixture<CartItemQuntityChangeComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ CartItemQuntityChangeComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(CartItemQuntityChangeComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
